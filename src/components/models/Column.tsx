/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.2.10 .\public\models\environment\column.glb -t
*/

import * as THREE from 'three';
import React, { useEffect, useMemo } from 'react';
import { useGLTF } from '@react-three/drei';
import { GLTF } from 'three-stdlib';

type GLTFResult = GLTF & {
  nodes: { column_1: THREE.Mesh };
  materials: { colormap: THREE.MeshStandardMaterial };
};

interface ColumnProps {
  tint?: number;
}

export function Column({ tint, ...props }: JSX.IntrinsicElements['group'] & ColumnProps) {
  const { nodes, materials } = useGLTF('/models/environment/column.glb') as GLTFResult;

  const material = useMemo(() => materials.colormap.clone(), [materials.colormap]);

  useEffect(() => {
    material.color.setScalar(tint ?? 1.0);
  }, [material, tint]);

  useEffect(() => {
    return () => { material.dispose(); };
  }, [material]);

  return (
    <group {...props} dispose={null}>
      <mesh geometry={nodes.column_1.geometry} material={material} />
    </group>
  );
}

useGLTF.preload('/models/environment/column.glb');
