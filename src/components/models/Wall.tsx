/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.2.10 ./public/models/environment/wall.glb -t
*/

import * as THREE from 'three';
import React, { useEffect, useMemo } from 'react';
import { useGLTF } from '@react-three/drei';
import { GLTF } from 'three-stdlib';

type GLTFResult = GLTF & {
  nodes: { wall_1: THREE.Mesh };
  materials: { colormap: THREE.MeshStandardMaterial };
};

interface WallProps {
  tint?: number;
}

export default function Wall({ tint, ...props }: JSX.IntrinsicElements['group'] & WallProps) {
  const { nodes, materials } = useGLTF('/models/environment/wall.glb') as GLTFResult;

  const material = useMemo(() => materials.colormap.clone(), [materials.colormap]);

  useEffect(() => {
    material.color.setScalar(tint ?? 1.0);
  }, [material, tint]);

  useEffect(() => {
    return () => { material.dispose(); };
  }, [material]);

  return (
    <group {...props} dispose={null}>
      <mesh geometry={nodes.wall_1.geometry} material={material} />
    </group>
  );
}

useGLTF.preload('/models/environment/wall.glb');
